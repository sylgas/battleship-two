angular.module('application.controllers')
    .controller('JoinGameController', ['$scope', '_', 'LoggedUser', 'JoinGameService',
        function ($scope, _, LoggedUser, JoinGameService) {

            var init = function() {

            };

            init();

        }
    ]);